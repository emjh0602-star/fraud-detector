# 작업 이력

> 최신 작업이 위에 옵니다. 새 대화에서 이 파일을 읽으면 마지막 작업 상태를 바로 파악할 수 있습니다.

---

## 2026-04-17 세션 2 - 코드 품질 개선

### 수행한 작업
1. **[크리티컬] pattern_stats NameError 수정**
   - `analyze_transactions()`에서 `global pattern_stats` 선언 추가
   - 이 변수를 `upload()` 함수에서 참조하는데, 이전에는 지역변수라 신규 거래 분석 시 서버 500 에러 발생
   - 위치: `app.py:689-691`

2. **[크리티컬] DATA_DIR 환경변수 적용**
   - `"data"` 하드코딩 → `os.environ.get("DATA_DIR", "data")`
   - 위치: `app.py:18`

3. **데드 코드 제거**
   - `smart_parse_excel()`의 `return all_rows` 이후 ~90줄 미도달 코드 삭제
   - 이 코드는 `parse_single_sheet()`로 이미 이동되어 있었음

4. **과거 데이터 중복 제거 로직 개선**
   - 기존: payee만으로 중복 체크 → 같은 거래처의 다른 금액/날짜 거래가 누락됨
   - 수정: payee + amount + date 조합으로 중복 체크
   - 위치: `app.py` upload 함수의 history 저장 부분 + cumul 저장 부분

5. **권한 체크 추가**
   - `delete_history`: 관리자 또는 해당 법인 담당자만 삭제 가능
   - `get_results` / `get_result_detail`: 관리자 또는 담당자만 조회 가능
   - 이전에는 `@login_required`만 있어서 아무 로그인 사용자가 모든 법인 데이터 접근 가능했음

6. **동일 수취인 "당일" 기준으로 수정**
   - 기존: 업로드 파일 전체에서 3건 이상이면 감지 (설계와 불일치)
   - 수정: 날짜별 수취인 건수로 감지 (`date_payee_count` dict 추가)
   - 날짜 없는 경우 기존 방식(전체 건수) 폴백

7. **비밀번호 해싱 강화**
   - SHA256 단순 해시 → werkzeug `generate_password_hash` (pbkdf2/scrypt + salt)
   - `verify_pw()` 함수로 기존 SHA256 해시 호환 + 로그인 시 자동 마이그레이션
   - 새 계정/비밀번호 변경 시에는 모두 안전한 해시 사용

8. **파일 동시 쓰기 보호**
   - `threading.Lock()` 추가, 모든 save 함수에 `_file_lock` 적용
   - 6명 동시 사용 시 JSON 파일 손상 방지

9. **corp_id 충돌 방지**
   - 타임스탬프만 → 타임스탬프 + `secrets.token_hex(4)` 랜덤값 추가

### 이 세션에서 아직 미커밋 상태
- 위 9개 수정사항이 로컬에만 있음
- 다음 대화에서 확인 후 push 필요

---

## 2026-04-17 세션 1 - 1차 설계 완료 (이전 Claude 대화에서 수행)
- 프로젝트 초기 구축 완료
- Flask 서버 + 순수 JS 프론트엔드 구성
- 엑셀 스마트 파서 구현 (자동 헤더 탐지, 컬럼 매핑)
- 이상거래 감지 규칙 11개 구현
- 인증/보안 시스템 구현 (로그인, 권한 분리, 감사 로그)
- 법인 관리 및 담당자 지정 기능
- Railway 배포 완료 (Volume 연결)
- 3개 법인 테스트 완료 (더편한샵, 디엔코스메틱스 등)

### 커밋 히스토리
- `ad763bd` 월평균 일평균 패턴 분석 추가
- `d94be76` 파일 미리보기 개선 및 결과 누적 저장
- `bc15ecc` 분석 결과 누적 저장 기능
- `108012c` UX 개선
- `caa4fdb` openpyxl read_only 오류 수정

---

## 다음 작업 예정
- [ ] 미커밋 코드 품질 개선 사항 push
- [ ] 나머지 51개 법인 엑셀 파일 분석 테스트
- [ ] 엑셀 양식별 파서 호환성 보완
- [ ] 시트 분리 도구 구현 (멀티시트 엑셀 대응)
